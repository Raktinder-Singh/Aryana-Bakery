<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Aryana" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/bottomnav</item>
    </style>

    <style name="Theme.Aryana" parent="Base.Theme.Aryana" />
    <style name="InActiveTextAppearance">
        <item name="android:textColor">@color/gray</item>
    </style>
    <style name="ActiveTextAppearance">
        <item name="android:textColor">@color/text</item>
    </style>
</resources>